using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ControlFireWorks : MonoBehaviour {

	//Rigidbody2D rb;
	//GameObject[] gmob = GameObject.FindGameObjectsWithTag("fuegos");
	// Use this for initialization
	void Start () {
		//gmob = gameObject.GetComponents<GameObject> ();
		//rb = GetComponent<Rigidbody2D> ();

	}
	
	// Update is called once per frame
	void FixedUpdate () {
		//gmob [0].SetActive (false);
		//rb.transform.position = 
	}
}
